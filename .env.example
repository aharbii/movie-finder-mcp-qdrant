# =============================================================================
# Qdrant RAG Evaluator - Debug vector embeddings, inspect payload filtering,
# and analyze semantic search distance during RAG development.
#
# Copy to `.env` before running any `make ...` target.
# Qdrant is external-only
# =============================================================================

# --- Qdrant Cloud ------------------------------------------------------------
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY_RO=
QDRANT_COLLECTION_NAME=movies

# --- OpenAI embeddings -------------------------------------------------------
OPENAI_API_KEY=sk-
