# movie-finder-mcp-qdrant (`mcp/qdrant-explorer`)

Internal MCP Server for Movie Finder.

This is a local developer tool that operates via `stdio`.
See `docs/internal/mcp-tooling.md` for the full developer contract and connection instructions.
